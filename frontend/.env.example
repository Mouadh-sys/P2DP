# API base URL (browser must reach the backend host)
VITE_API_BASE_URL=http://localhost:8000

# Jaeger UI (browser opens this host; default matches Docker Compose)
VITE_JAEGER_UI_URL=http://localhost:16686
